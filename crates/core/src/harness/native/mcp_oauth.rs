//! MCP-specific OAuth discovery — RFC 9728 Protected Resource Metadata and
//! the RFC 8707 `resource` parameter — layered on the OAuth primitives
//! already shipped in [`crate::plugins::oauth`].
//!
//! Intentionally empty until Task 4 of
//! `docs/design/2026-08-10-remote-mcp-http-oauth-plan.md` fills it in.
