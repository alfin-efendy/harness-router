---
description: Review the current working changes for bugs and issues.
agent: plan
surfaces: session
---
Review the current working changes (run `git diff` and, if empty, `git diff --staged`). Report correctness bugs, risky changes, and obvious improvements, grouped by severity. Do not modify files. $ARGUMENTS